server:
  port: 8080

spring:
  mvc:
    log-request-details: true

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

realworld:
  jwt.key: some-key

logging.level:
  web: debug
  sql: debug
  cn.sabercon.realworld: debug
